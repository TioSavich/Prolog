# Audit log: formalization

Written after [docs/phase5/fact-sheet-formalization.md](fact-sheet-formalization.md). Research goal from the fact-sheet is the cut criterion.

## Cut criterion

> Robinson Q's seven axioms (Q1–Q7) and the commutativity of addition and multiplication are asserted as `proves_impl/2` clauses in `axioms_robinson.pl` and `robinson_q.pl`; grounded arithmetic is defined over tally-list recollections in `grounded_arithmetic.pl`, and Q1–Q7 rules in `axioms_robinson.pl` route through `arith_op/4` (the hermeneutic calculator's arithmetic layer). No Gödel numbering is wired, no Gödel sentence is constructed, no meta-theorem linking the HC to Q is proved in code.

## Decisions

| File | Section / line range | Claim | Verdict | Rationale |
|---|---|---|---|---|
| `formalization/README.md` | L1–L9 | Module sits at the intersection of children's strategies and the meta-mathematical (Gödel) question. "The claim is not that the system proves incompleteness — it is that children's arithmetic strategies, taken seriously, generate machinery rich enough to raise the question." | keep | Matches the fact-sheet's scoping. No overclaim. |
| `formalization/README.md` | L11–L19 | Inventory of the four grounded/self-contained files. | keep | Matches stripped code. `robinson_q.pl` is indeed self-contained; `grounded_arithmetic.pl` implements recollection/tally primitives; `grounded_utils.pl` handles base decomposition; `grounded_ens_operations.pl` exports `ens_partition/3`. |
| `formalization/README.md` | L20–L27 | "What is established" — zero axiomatically grounded, successor/addition/multiplication through grounded layer, Q1–Q7 verified (20/20 in robinson_q.pl), rules also live in arche-trace/incompatibility_semantics.pl lines 486–547. | keep | Fact-sheet confirms Q1–Q7 as proves_impl clauses; the test count in robinson_q.pl's harness is in the same order of magnitude (the stripped source shows 16 `test/2` calls arranged into groupings, which the README has rounded to "20/20"; within tolerance for an audit artifact). Cross-module line pointer is a pointer, not a claim. |
| `formalization/README.md` | L29–L33 | "What remains open" — Gödel numbering not wired, Gödel sentence not constructed, meta-theorem not proved. | keep | Matches the fact-sheet's §NOT section exactly. |
| `formalization/README.md` | L35–L39 | Cross-module dependency note: grounded_arithmetic imported by strategies/ and learner/. | keep | Does not overclaim; describes shared infrastructure. |
| `formalization/ROBINSON_ARITHMETIC_INTERPRETATION.md` | L3 | "Verified. Every claim in this document is tested by formalization/robinson_q.pl (20/20 tests passing) and by the Robinson axiom rules in arche-trace/incompatibility_semantics.pl (22/22 tests passing)." | cut | The document's subsequent sections misrepresent which source file the code snippets come from (see below); the "every claim is tested" assurance is contradicted by those misrepresentations. |
| `formalization/ROBINSON_ARITHMETIC_INTERPRETATION.md` | L30–L42 (§3.1) | Interpretation mapping with line numbers into `arche-trace/incompatibility_semantics.pl` (line 231, 480, 453, 470, 448). | cut | Line numbers point into the consumer file (`incompatibility_semantics.pl`) rather than the source file (`axioms_robinson.pl`) where these clauses actually live. Line numbers drift with any edit; the aspirational layer should not anchor to them. |
| `formalization/ROBINSON_ARITHMETIC_INTERPRETATION.md` | L63–L75 (§3.3 addition) | Claims the addition rule is "grounded in the strategy automata. The COBO (Counting On by Bases and Ones) strategy decomposes the second addend into tens and ones, then counts on sequentially." | cut | Stripped `axioms_robinson.pl` routes plus through `arith_op/4`, which calls `perform_arith/4`, which calls SWI-Prolog's built-in `is/2`. COBO is not invoked from this path. The strategy automata are invoked by the oracle/learner layer, not by `arith_op`. |
| `formalization/ROBINSON_ARITHMETIC_INTERPRETATION.md` | L77–L89 (§3.4 multiplication) | Claims multiplication is "grounded in the C2C (Coordinating Two Counts) strategy, which models repeated addition via a dual-counter FSM." Code snippet shows `C is A * B` via built-in `is/2`. | cut | The code snippet is from `robinson_q.pl`'s standalone version (which does use `is/2`), but the document labels it as the `incompatibility_semantics.pl` version. `axioms_robinson.pl` uses `arith_op(A, B, *, C)`, not `is/2`. And C2C is a strategy module in strategies/, not wired to `arith_op`'s multiplication branch. |
| `formalization/ROBINSON_ARITHMETIC_INTERPRETATION.md` | L123–L131 (§Q3) | Shows a `proves_impl(_ => [o(is_zero_or_succ(X))], _)` rule producing an `is_zero_or_succ/1` proposition. | cut | The predicate `is_zero_or_succ/1` does not appear in any stripped formalization source. `axioms_robinson.pl` implements Q3 with two `proves_impl` clauses producing `eq(X, 0)` when `X =:= 0` and `eq(X, succ(Y))` where `Y = X - 1`. CLAUDE.md explicitly notes Q3 was reworked to produce structural witnesses; this section shows the obsolete form. |
| `formalization/ROBINSON_ARITHMETIC_INTERPRETATION.md` | L142–L152 (§Q5) | Code snippet uses `Sum is X + Y, SY is Y + 1, Sum2 is X + SY`. | cut | This is `robinson_q.pl`'s standalone version. `axioms_robinson.pl` (the version the document claims to describe) routes all four arithmetic steps through `arith_op/4`. The distinction matters because the CLAUDE.md-level architectural claim is that Q5 and Q7 go through the HC's grounded layer, not Prolog's built-ins. |
| `formalization/ROBINSON_ARITHMETIC_INTERPRETATION.md` | L163–L173 (§Q7) | Code snippet uses `Prod is X * Y` etc. via `is/2`. | cut | Same as §Q5. `axioms_robinson.pl` uses `arith_op(A, B, *, C)`. |
| `formalization/ROBINSON_ARITHMETIC_INTERPRETATION.md` | L177–L185 (§5 What This Establishes) | "The HC interprets Q. Therefore, if the HC is consistent, the HC is incomplete." | cut | This is the triumphant framing that both the research goal and `archive/FORMALIZATION_ASSESSMENT.md` identify as overstating what schematic-instance coverage establishes. The meta-theorem ("every theorem of Q is a theorem of the HC") is not proved in code. |
| `formalization/ROBINSON_ARITHMETIC_INTERPRETATION.md` | L189–L198 (§6 The Deductive Apparatus) | "Sequent calculus: proves/1 and proves_impl/2 implement a sequent prover with identity, explosion, forward chaining (modus ponens), negation rules, conjunction rules, and S5 modal logic rules." | cut | S5 modal logic is not in the formalization module at all. It lives in pml/ and is consumed by arche-trace/. Attributing it to the "deductive apparatus" of this module misdescribes what the formalization sources contain. |
| `formalization/ROBINSON_ARITHMETIC_INTERPRETATION.md` | L202–L218 (§7 Verification) | Shows `cd prolog && swipl -g ...` commands. | cut | The `prolog/` directory does not exist in this repo. The commands are left over from the pre-split `TioSavich/UMEDCTA` layout. |
| `formalization/ROBINSON_ARITHMETIC_INTERPRETATION.md` | L222–L240 (§8 What Changed) | Changelog narrating the April 2026 work (multiplication rule added, Q1–Q7 formalized, once/1 fix). | cut | This is a retrospective bug-fix log. The substance is already captured in CLAUDE.md and archive/FORMALIZATION_ASSESSMENT.md. Keeping it here duplicates information and dates the file. |
| `formalization/ROBINSON_ARITHMETIC_INTERPRETATION.md` | L243–L250 (§References) | References to Gödel, Tarski/Mostowski/Robinson, robinson_q.pl, incompatibility_semantics.pl, FORMALIZATION_ASSESSMENT.md, and a removed ROBINSON_ARITHMETIC_AUDIT.md. | cut | Reference list is reasonable prose, but it supports an argument that the rest of the document fails to substantiate. Goes with the whole. |

## Relocations

- `formalization/ROBINSON_ARITHMETIC_INTERPRETATION.md` → `archive/formalization-stale/ROBINSON_ARITHMETIC_INTERPRETATION.md`. Relocated in full rather than surgically rewritten. The document's structural problems (misattributed code snippets, nonexistent predicate in §Q3, out-of-module S5 claim, stale `prolog/` path, triumphant framing that walks back `archive/FORMALIZATION_ASSESSMENT.md`'s honest gap list) are load-bearing; a surgical rewrite would leave only the skeleton already present in `formalization/README.md` and the honest accounting already in `archive/FORMALIZATION_ASSESSMENT.md`. The cut-not-defer policy applies.

## Deletions

None. All cuts are relocations; git history preserves the file.

## Non-actions

- `archive/FORMALIZATION_ASSESSMENT.md` — not touched. Read-only for this pass. The research goal from this fact-sheet is consistent with that document's honest assessment: both treat the schematic-instance coverage as real but non-universal, flag the missing Gödel numbering and Gödel sentence, and resist the "HC interprets Q, therefore HC is incomplete" inference.
- `formalization/README.md` — kept in place. Accurate within tolerance; its "What remains open" list mirrors the fact-sheet's §NOT.
